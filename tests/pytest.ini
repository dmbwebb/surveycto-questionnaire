[pytest]
cache_dir = /Users/duncanwebb/.cache/pytest/surveycto_questionnaire_skill
testpaths = .
addopts = -ra --strict-markers
markers =
    live: hits real Google Drive/Sheets APIs against test fixtures (slow, requires OAuth)
    destructive: mutates fixture sheets (uses ephemeral copies; fixtures are cleaned up)
